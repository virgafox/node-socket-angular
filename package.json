{
  "name": "sync_test",
  "version": "0.0.1",
  "description": "Real-time sync between angular interfaces, linked via QRcode.",
  "main": "server.js",
  "repository": {
    "type": "git",
    "url": "bitbucket:virgafox/sync"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "nodemon": "nodemon server.js",
    "postinstall": "bower cache clean && bower install"
  },
  "author": "Marco Virgadamo",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.5.2",
    "express": "^4.18.2",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.4.0"
  }
}
